"""Web-based front-end for RSID Face Guard.

Hosts the designer-provided web UI (demo_ui/) inside a QtWebEngine view,
reusing the shared business/hardware layers (AuthService,
PreviewController, UserDatabase, hardware/*) unchanged.
"""