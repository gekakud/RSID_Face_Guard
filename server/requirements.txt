# Dashboard / provisioning server. Independent of the device's requirements.txt
# (no PySide6, no lgpio, no rsid_py) so this deploys to a plain Linux host.
fastapi
uvicorn[standard]
jinja2
pydantic
cryptography
qrcode[pil]
requests

# Used to read back each QR we generate and confirm the device's own decoder
# (pyzbar/zbar) can actually parse it before we hand it to an installer. See
# signing._decodes(). pyzbar needs the system library libzbar0; where it's
# unavailable (e.g. a plain Render Python runtime) the self-check degrades to
# "skip" rather than failing. numpy is also used by the device-compatibility
# tests, which import qr_scanner.
numpy
pyzbar

# Dev / test only
pytest
httpx
